#include "farsh.h"
/**
* _uppercase - checks for uppercase character
* @q: Variable text
* Return: Always 0.
*/
int _uppercase(int q)
{
	if (q >= 'A' && q <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

